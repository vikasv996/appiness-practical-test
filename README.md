### Appiness Practical Test

##### Steps
- Clone the repository (code).
- Run `db_export.sql` file for creation of Database, its tables and sample data.
- Run the application with `NODE_ENV=development node app.js`.
- There are two APIS: `/api/list-categories` for listing categories and `/api/list-products/1` for listing products associated with each category.
